# 🛠️ أدوات المطور (Developer Toolkit)

<div dir="rtl">

تم تجهيز **Haitham Voice Agent (HVA)** بمجموعة أدوات متقدمة للمطورين مدمجة مباشرة في التطبيق، تهدف إلى تسهيل عملية التطوير، وتصحيح الأخطاء، ومراقبة أداء النظام في الوقت الفعلي.

للوصول إلى هذه الأدوات، انتقل إلى صفحة **"أدوات المطور"** (Developer Toolkit) من القائمة الجانبية (أيقونة 📜).

</div>

## 🌟 المميزات الرئيسية | Key Features

### 1. 🌐 مراقب الشبكة (Network Monitor)

<div dir="rtl">

يعمل هذا النظام كـ "Interceptor" لجميع طلبات الشبكة الصادرة من الواجهة الأمامية (Frontend) إلى الخادم (Backend).

*   **المراقبة الحية**: عرض فوري لكل طلب API (GET, POST, etc.).
*   **التفاصيل الكاملة**: عند النقر على أي طلب، تظهر نافذة منبثقة تعرض:
    *   **URL & Method**: الرابط وطريقة الطلب.
    *   **Status Code**: حالة الاستجابة (200, 404, 500, etc.) مع ترميز لوني.
    *   **Duration**: المدة الزمنية التي استغرقها الطلب بالمللي ثانية.
    *   **Request & Response Body**: عرض محتوى JSON المرسل والمستقبل بتنسيق مقروء.

</div>

### 2. 🩺 المشخص الذكي (Smart Diagnostics)

<div dir="rtl">

نظام ذكي لتحليل الأخطاء وتوفير حلول فورية.

*   **تحليل السجلات**: عند النقر على أي سجل خطأ (Error) أو تحذير (Warning)، يقوم النظام بتحليله.
*   **تحديد الموقع التقني (Technical Location)**: يستخرج النظام تلقائياً اسم الملف، رقم السطر، واسم الدالة التي تسببت في الخطأ (سواء في Python أو JavaScript).
*   **التوصيات**: يقدم شرحاً للمشكلة، تأثيرها المحتمل، وخطوات مقترحة للحل باللغة العربية.

</div>

### 3. 📊 فاحص الحالة (State Inspector)

<div dir="rtl">

لوحة قيادة لمراقبة الحالة الداخلية للنظام في الوقت الفعلي.

*   **WebSocket Status**: يوضح حالة الاتصال بالخادم (Connected/Disconnected) وحالة الاستماع للأوامر الصوتية (Active/Idle).
*   **Memory Stats**: يعرض إحصائيات حية عن الذاكرة (عدد الذكريات المخزنة، المجموعات النشطة).

</div>

### 4. 📦 تصدير تقرير التصحيح (Debug Report Export)

<div dir="rtl">

ميزة حيوية لمشاركة تفاصيل المشاكل مع فريق التطوير أو للتحليل اللاحق.

*   **زر "تصدير تقرير"**: بضغطة واحدة، يتم توليد ملف JSON شامل يحتوي على:
    *   معلومات النظام (User Agent, Platform).
    *   الحالة الحالية للنظام (State Snapshot).
    *   سجلات الواجهة الأمامية والخلفية كاملة.
    *   سجل كامل لطلبات الشبكة وتفاصيلها.
*   **اسم الملف**: يتم تسمية الملف تلقائياً بطابع زمني لسهولة التنظيم (مثلاً: `hva-debug-report-1733139283.json`).

</div>

---

## 📸 لقطات الشاشة | Screenshots

### واجهة أدوات المطور الرئيسية
*(يمكن إضافة لقطة شاشة هنا)*

### تفاصيل طلب الشبكة
*(يمكن إضافة لقطة شاشة هنا)*

### نافذة التشخيص الذكي
*(يمكن إضافة لقطة شاشة هنا)*

---

## 💡 كيفية الاستخدام | How to Use

<div dir="rtl">

1.  **فتح الأدوات**: اضغط على أيقونة "السجلات" في القائمة الجانبية.
2.  **التنقل بين التبويبات**: استخدم الأزرار العلوية (الواجهة، الخادم، الشبكة، الحالة) للتنقل بين الأدوات المختلفة.
3.  **فحص خطأ**: إذا رأيت خطأ باللون الأحمر في تبويب "الواجهة" أو "الخادم"، اضغط عليه لفتح نافذة التشخيص.
4.  **مراقبة API**: انتقل لتبويب "الشبكة" لرؤية الطلبات الحية. اضغط على أي طلب لرؤية البيانات المرسلة والمستقبلة.
5.  **تصدير البيانات**: في حال واجهت مشكلة غامضة، اضغط "تصدير تقرير" وأرسل الملف للمطور المسؤول.

</div>
